describe('calculator', () => {
    it('should have result as 20 for addition of 12 + 8', () => {
        expect(12 + 8).toEqual(20)
    })

    it('should have result as 20 for addition of 12 + 8', () => {
        expect(12 + 8).toEqual(20)
    })

    it('should have result as 20 for addition of 12 + 8', () => {
        expect(12 + 8).toEqual(20)
    })

    it('should have result as 20 for addition of 12 + 8', () => {
        expect(12 + 8).toEqual(20)
    })

    it('should have result as 20 for addition of 12 + 8', () => {
        expect(12 + 8).toEqual(20)
    })
});